// Simple TensorLogic program

main {
    // Basic arithmetic
    x := 10
    y := 20
    sum := x + y
    product := x * y
    
    // Control flow
    if sum > 25 {
        result := 1
    } else {
        result := 0
    }
    
    // Loop
    total := 0
    for i in range(5) {
        total := total + i
    }
}
